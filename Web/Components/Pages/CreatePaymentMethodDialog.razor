@using Application.DTO
@using Application.Services.PaymentMethods
@using MudBlazor
@inject NavigationManager NavigationManager
@inject IPaymentMethodService PaymentMethodService

<MudDialog Style="border-radius:12px; overflow:hidden;">

    <!-- ══════════ HEADER BANNER ══════════ -->
    <DialogContent>
        <MudPaper Elevation="0"
                  Style="background: linear-gradient(135deg, #1976D2 0%, #42A5F5 100%);
                         border-radius:8px; padding:14px 18px; margin-bottom:16px;">
            <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2">
                <MudIcon Icon="@Icons.Material.Filled.CreditCard"
                         Style="color:white; font-size:1.6rem;" />
                <MudStack Spacing="0">
                    <MudText Typo="Typo.subtitle1"
                             Style="color:white; font-weight:700; line-height:1.2;">
                        New Payment Method
                    </MudText>
                    <MudText Typo="Typo.caption"
                             Style="color:rgba(255,255,255,0.80);">
                        Add a new method used for receiving payments
                    </MudText>
                </MudStack>
            </MudStack>
        </MudPaper>

        <!-- ══════════ FORM ══════════ -->
        <EditForm Model="Model" OnValidSubmit="OnValidSubmit">
            <DataAnnotationsValidator />
            <ValidationSummary />

            <MudStack Spacing="1">

                <!-- ── Section Label ── -->
                <MudText Typo="Typo.caption"
                         Style="color:#9E9E9E; font-weight:600; letter-spacing:.08em;
                                text-transform:uppercase; margin-bottom:2px;">
                    Method Details
                </MudText>

                <!-- Payment Method Name -->
                <MudTextField T="string"
                              Label="Payment Method Name"
                              Variant="Variant.Outlined"
                              Margin="Margin.Dense"
                              @bind-Value="Model.PaymentMethodName"
                              Placeholder="e.g. Cash, Bank Transfer, PayPal…"
                              Adornment="Adornment.Start"
                              AdornmentIcon="@Icons.Material.Filled.Payments"
                              AdornmentColor="Color.Primary"
                              For="@(() => Model.PaymentMethodName)" />
                <ValidationMessage For="@(() => Model.PaymentMethodName)" />

            </MudStack>
        </EditForm>
    </DialogContent>

    <!-- ══════════ ACTIONS ══════════ -->
    <DialogActions>
        <MudStack Row="true" Spacing="2" Class="px-2 pb-2" Style="width:100%;">

           

            <MudButton Variant="Variant.Filled"
                       Color="Color.Primary"
                       Size="Size.Small"
                       StartIcon="@Icons.Material.Filled.Save"
                       OnClick="OnValidSubmit"
                       Style="flex:2; border-radius:8px;
                              background: linear-gradient(135deg,#1976D2,#42A5F5);">
                Save Method
            </MudButton>

        </MudStack>
    </DialogActions>

</MudDialog>

@code {
    [CascadingParameter]
    private IDialogReference DialogReference { get; set; } = default!;

    private CreatePaymentMethodDTO Model { get; set; } = new();

    [Parameter]
    public EventCallback OnPaymentMethodCreated { get; set; }

    private bool ShowErrors { get; set; } = false;

    private async Task OnValidSubmit()
    {
        try
        {
            await PaymentMethodService.CreatePaymentMethodAsync(Model);

            // Notify parent to reload dropdown list
            await OnPaymentMethodCreated.InvokeAsync();

              Model = new CreatePaymentMethodDTO();

            @* DialogReference.Close(DialogResult.Ok(true)); *@
        }
        catch
        {
            ShowErrors = true;
        }

       
    }

    private void Cancel()
    {
        DialogReference.Close(DialogResult.Cancel());
    }
}